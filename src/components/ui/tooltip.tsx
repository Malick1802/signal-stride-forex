import React from 'react';

// Tooltip component removed to resolve React conflicts
// Use native title attribute for basic tooltips if needed

export const Tooltip = ({ children }: { children: React.ReactNode }) => <>{children}</>;
export const TooltipTrigger = ({ children }: { children: React.ReactNode }) => <>{children}</>;
export const TooltipContent = () => null;
export const TooltipProvider = ({ children }: { children: React.ReactNode }) => <>{children}</>;